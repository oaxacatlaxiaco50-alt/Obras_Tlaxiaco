export const environment = {
  production: false,
  // Detects the host automatically — works on any network (LAN, WiFi, or production internet)
  apiUrl: `http://${window.location.hostname}:8081`
};
